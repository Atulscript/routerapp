---
import BaseLayout from '../layouts/BaseLayout.astro';
import PasswordTable from '../components/PasswordTable.astro';
import AdBanner from '../components/AdBanner.astro';
import RelatedLinksGrid from '../components/RelatedLinksGrid.astro';
import RouterGuideCard from '../components/RouterGuideCard.astro';
import { getAllSlugs, getBrandSlug, getIpSlug, getModelSlug, getModelsForIp, getModelsForBrand, formatModelTitle, GATEWAY_IPS, BRANDS, ISPS, ROUTER_MODELS } from '../lib/data-loader';
import type { GatewayIp, Brand, Isp, RouterModel } from '../lib/types';

export async function getStaticPaths() {
  return await getAllSlugs();
}

type Props = 
  | { type: 'ip'; data: GatewayIp }
  | { type: 'brand'; data: Brand }
  | { type: 'isp'; data: Isp }
  | { type: 'model'; data: RouterModel };

const { type, data } = Astro.props as Props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');

// Dynamic title, description, keywords, and schemas based on page type
let pageTitle = "";
let pageDescription = "";
let keywords: string[] = [];
let schemas: Record<string, any>[] = [];
const siteUrl = "https://atulscript.github.io/routerapp";
const currentCanonical = `${siteUrl}/${data.slug}`;

if (type === 'ip') {
  const ipData = data as GatewayIp;
  pageTitle = `${ipData.ip} Router Admin Login, Passwords & Setup Guide | 19216811.page`;
  pageDescription = `Access router admin dashboard at ${ipData.ip}. Default login usernames, factory passwords, WiFi security configuration, and troubleshooting for ${ipData.commonBrands.slice(0, 4).join(', ')}.`;
  keywords = [
    ipData.ip,
    `${ipData.ip} login`,
    `${ipData.ip} admin`,
    `${ipData.ip} default password`,
    `${ipData.ip} router setup`,
    "default gateway ip",
    "router admin dashboard",
    ...ipData.commonBrands.map(b => `${b} router login`)
  ];

  schemas = [
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Home",
          "item": `${siteUrl}/`
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": "Gateway IP Addresses",
          "item": `${siteUrl}/ip-addresses`
        },
        {
          "@type": "ListItem",
          "position": 3,
          "name": `${ipData.ip} Setup Guide`,
          "item": currentCanonical
        }
      ]
    },
    {
      "@type": "HowTo",
      "name": `How to Login to Router Gateway at ${ipData.ip}`,
      "description": `Step-by-step instructions to connect and log into your router dashboard at ${ipData.ip}.`,
      "step": [
        {
          "@type": "HowToStep",
          "position": 1,
          "name": "Connect Network Cable or Wi-Fi",
          "text": "Connect your computer or phone to the router via Ethernet or Wi-Fi.",
          "url": `${currentCanonical}#step-1`
        },
        {
          "@type": "HowToStep",
          "position": 2,
          "name": `Navigate to http://${ipData.ip}`,
          "text": `Open your browser address bar, type http://${ipData.ip} and press Enter.`,
          "url": `${currentCanonical}#step-2`
        },
        {
          "@type": "HowToStep",
          "position": 3,
          "name": "Enter Credentials",
          "text": `Enter default username (${ipData.defaultUser}) and password (${ipData.defaultPass}) to access admin settings.`,
          "url": `${currentCanonical}#step-3`
        }
      ]
    },
    {
      "@type": "TechArticle",
      "headline": pageTitle,
      "description": pageDescription,
      "url": currentCanonical,
      "author": {
        "@type": "Organization",
        "name": "19216811.page Network Research"
      },
      "articleBody": ipData.steps.join(' ')
    },
    {
      "@type": "FAQPage",
      "mainEntity": ipData.faqs.map(faq => ({
        "@type": "Question",
        "name": faq.question,
        "acceptedAnswer": {
          "@type": "Answer",
          "text": faq.answer
        }
      }))
    }
  ];
} else if (type === 'brand') {
  const brandData = data as Brand;
  pageTitle = `${brandData.name} Router Default Passwords & Admin Login Guide | 19216811.page`;
  pageDescription = `Default router passwords, factory usernames, and admin dashboard login IP (${brandData.defaultIp}) for ${brandData.name} wireless routers, modems, and mesh systems.`;
  keywords = [
    brandData.name,
    `${brandData.name} router login`,
    `${brandData.name} default password`,
    `${brandData.name} admin login`,
    `${brandData.name} default ip`,
    `${brandData.name} wifi setup`,
    "router passwords directory"
  ];

  schemas = [
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Home",
          "item": `${siteUrl}/`
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": "Router Brands",
          "item": `${siteUrl}/brands`
        },
        {
          "@type": "ListItem",
          "position": 3,
          "name": `${brandData.name} Routers`,
          "item": currentCanonical
        }
      ]
    },
    {
      "@type": "HowTo",
      "name": `How to Configure ${brandData.name} Wireless Router`,
      "description": `Step-by-step setup and admin password guide for ${brandData.name} hardware.`,
      "step": brandData.guide.map((step, idx) => ({
        "@type": "HowToStep",
        "position": idx + 1,
        "name": `Step ${idx + 1}`,
        "text": step,
        "url": `${currentCanonical}#step-${idx + 1}`
      }))
    },
    {
      "@type": "TechArticle",
      "headline": pageTitle,
      "description": pageDescription,
      "url": currentCanonical,
      "author": {
        "@type": "Organization",
        "name": "19216811.page Network Research"
      },
      "articleBody": brandData.description
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": `What is the default IP address for ${brandData.name} routers?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `Most ${brandData.name} routers use ${brandData.defaultIp} as their factory default gateway management address.`
          }
        },
        {
          "@type": "Question",
          "name": `What is the factory default login password for ${brandData.name}?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `The standard factory username is "${brandData.defaultUser}" and default password is "${brandData.defaultPass}". Check your physical router label if these don't work.`
          }
        },
        {
          "@type": "Question",
          "name": `How do I factory reset my ${brandData.name} router if I forgot the password?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `Press and hold the physical RESET pinhole button on the back of your ${brandData.name} device with a paperclip for 10-15 seconds until all LED lights blink, restoring default credentials.`
          }
        }
      ]
    }
  ];
} else if (type === 'isp') {
  const ispData = data as Isp;
  pageTitle = `${ispData.name} Router Admin Login, Default Password & FTTH Setup | 19216811.page`;
  pageDescription = `Complete setup guide for ${ispData.name} optical fiber home gateways. Default login credentials, IP address (${ispData.defaultIp}), optical power troubleshooting, and Wi-Fi security.`;
  keywords = [
    ispData.name,
    `${ispData.name} router login`,
    `${ispData.name} default password`,
    `${ispData.name} admin ip`,
    `${ispData.name} fiber setup`,
    `${ispData.name} ont settings`,
    "isp router configuration"
  ];

  schemas = [
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Home",
          "item": `${siteUrl}/`
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": `${ispData.name} Guide`,
          "item": currentCanonical
        }
      ]
    },
    {
      "@type": "HowTo",
      "name": `How to Set Up ${ispData.name} Fiber Home Gateway`,
      "description": `Step-by-step instructions for ${ispData.name} broadband routers.`,
      "step": ispData.instructions.map((step, idx) => ({
        "@type": "HowToStep",
        "position": idx + 1,
        "name": `Step ${idx + 1}`,
        "text": step,
        "url": `${currentCanonical}#step-${idx + 1}`
      }))
    },
    {
      "@type": "TechArticle",
      "headline": pageTitle,
      "description": pageDescription,
      "url": currentCanonical,
      "author": {
        "@type": "Organization",
        "name": "19216811.page Network Research"
      },
      "articleBody": ispData.description
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": `How do I log in to my ${ispData.name} fiber router admin portal?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `Connect to your ${ispData.name} Wi-Fi network, navigate to http://${ispData.defaultIp} in your browser, and enter username "${ispData.defaultUser}" and password "${ispData.defaultPass}".`
          }
        },
        {
          "@type": "Question",
          "name": `What should I do if the LOS light on my ${ispData.name} router is blinking red?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `A blinking red LOS (Loss of Signal) light indicates an optical fiber cable disconnection or an ISP outage. Verify the thin yellow fiber cable is firmly seated, and contact ${ispData.name} support if the issue persists.`
          }
        },
        {
          "@type": "Question",
          "name": `How can I change my ${ispData.name} Wi-Fi password and network name?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `Log in to http://${ispData.defaultIp}, go to Network/WLAN -> 2.4G/5G Settings, update the SSID and WPA Pre-Shared Key, then click Apply.`
          }
        }
      ]
    }
  ];
} else if (type === 'model') {
  const modelData = data as RouterModel;
  pageTitle = `${modelData.brand} ${modelData.model} Default Password, Admin Login & Specs | 19216811.page`;
  pageDescription = `Factory default login credentials, IP address (${modelData.ip}), Wi-Fi specs, port configuration, and setup guide for ${modelData.brand} ${modelData.model} (${modelData.category}).`;
  keywords = [
    modelData.model,
    `${modelData.brand} ${modelData.model}`,
    `${modelData.brand} ${modelData.model} login`,
    `${modelData.brand} ${modelData.model} default password`,
    `${modelData.brand} ${modelData.model} specs`,
    `${modelData.ip} admin`,
    modelData.category,
    modelData.series || '',
    "router hardware database"
  ];

  schemas = [
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Home",
          "item": `${siteUrl}/`
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": "Router Models",
          "item": `${siteUrl}/routers`
        },
        {
          "@type": "ListItem",
          "position": 3,
          "name": `${modelData.brand} ${modelData.model}`,
          "item": currentCanonical
        }
      ]
    },
    {
      "@type": "Product",
      "name": `${modelData.brand} ${modelData.model}`,
      "description": modelData.useCase,
      "brand": {
        "@type": "Brand",
        "name": modelData.brand
      },
      "category": modelData.category
    },
    {
      "@type": "HowTo",
      "name": `How to Login and Setup ${modelData.brand} ${modelData.model}`,
      "description": `Step-by-step instructions to configure ${modelData.brand} ${modelData.model} router.`,
      "step": [
        {
          "@type": "HowToStep",
          "position": 1,
          "name": "Connect Network Cable or Wi-Fi",
          "text": `Connect your PC or mobile phone to the ${modelData.brand} ${modelData.model} via Ethernet cable or default wireless network.`,
          "url": `${currentCanonical}#step-1`
        },
        {
          "@type": "HowToStep",
          "position": 2,
          "name": `Open http://${modelData.ip}`,
          "text": `Open your internet browser and type http://${modelData.ip} in the address bar.`,
          "url": `${currentCanonical}#step-2`
        },
        {
          "@type": "HowToStep",
          "position": 3,
          "name": "Enter Default Credentials",
          "text": `Enter username "${modelData.username}" and password "${modelData.password}" to access the management dashboard.`,
          "url": `${currentCanonical}#step-3`
        }
      ]
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": `What is the default IP address for ${modelData.brand} ${modelData.model}?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `The factory default IP address for ${modelData.brand} ${modelData.model} is http://${modelData.ip}.`
          }
        },
        {
          "@type": "Question",
          "name": `What is the default username and password for ${modelData.brand} ${modelData.model}?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `The default username is "${modelData.username}" and the default password is "${modelData.password}".`
          }
        },
        {
          "@type": "Question",
          "name": `How do I factory reset the ${modelData.brand} ${modelData.model}?`,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": `Locate the small Reset pinhole on the back panel of the ${modelData.brand} ${modelData.model}. Press and hold it with a paperclip for 10-15 seconds while powered on until the LED lights cycle.`
          }
        }
      ]
    }
  ];
}
---

<BaseLayout 
  title={pageTitle} 
  description={pageDescription}
  keywords={keywords}
  canonical={currentCanonical}
  schema={schemas}
>
  <div class="max-w-5xl mx-auto px-4 sm:px-6 py-6 sm:py-8 space-y-6">

    <!-- Top Leaderboard Ad Slot -->
    <AdBanner format="leaderboard" />

    {/* ========================================================= */}
    {/* VIEW 1: GATEWAY IP PAGE */}
    {/* ========================================================= */}
    {type === 'ip' && (() => {
      const ipData = data as GatewayIp;
      const associatedModels = getModelsForIp(ipData.ip);
      const otherIps = GATEWAY_IPS.filter(i => i.ip !== ipData.ip);

      return (
        <article class="space-y-6">
          {/* Breadcrumb */}
          <nav class="flex items-center gap-1.5 text-xs text-[#5f6368] dark:text-[#9aa0a6]">
            <a href={`${base}/`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">Home</a>
            <span>/</span>
            <a href={`${base}/ip-addresses`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">IP Gateways</a>
            <span>/</span>
            <span class="text-[#202124] dark:text-[#e8eaed] font-medium">{ipData.ip}</span>
          </nav>

          {/* Hero Header Card (Google OneBox Style) */}
          <header class="g-card p-5 sm:p-7 space-y-5">
            <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-[#e8eaed] dark:border-[#3c4043] pb-4">
              <div>
                <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full text-xs font-bold bg-[#e8f0fe] dark:bg-[#1a3a60] text-[#1a73e8] dark:text-[#8ab4f8] border border-[#d2e3fc] dark:border-[#1a73e8] mb-2">
                  <span class="w-2 h-2 rounded-full bg-[#34a853] animate-pulse"></span>
                  Default Gateway IP
                </div>
                <h1 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-[#202124] dark:text-white tracking-tight">
                  {ipData.ip} Router Admin Login
                </h1>
                <p class="text-xs sm:text-sm text-[#5f6368] dark:text-[#9aa0a6] mt-1.5 max-w-2xl leading-relaxed">
                  {ipData.description}
                </p>
              </div>

              <div class="flex-shrink-0">
                <a 
                  href={`http://${ipData.ip}`} 
                  target="_blank" 
                  rel="noopener noreferrer"
                  class="g-btn-primary"
                >
                  <span>Open http://{ipData.ip}</span>
                  <span class="text-xs">↗</span>
                </a>
              </div>
            </div>

            {/* Quick Metrics */}
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Gateway IP</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">{ipData.ip}</div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={ipData.ip}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Default Username</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">{ipData.defaultUser || '(blank)'}</div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={ipData.defaultUser}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Default Password</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">{ipData.defaultPass || '(blank)'}</div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={ipData.defaultPass}
                >
                  📋 Copy
                </button>
              </div>
            </div>
          </header>

          {/* Universal 3-Step Setup Guide */}
          <RouterGuideCard 
            entityName={ipData.ip} 
            ipAddress={ipData.ip}
            brandName={ipData.commonBrands[0] || 'Router'} 
          />

          {/* Associated Router Models */}
          {associatedModels.length > 0 && (
            <section class="g-card p-5 sm:p-6 space-y-4">
              <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-[#e8eaed] dark:border-[#3c4043] pb-3">
                <div>
                  <h2 class="text-base sm:text-lg font-bold text-[#202124] dark:text-white flex items-center gap-2">
                    <span>📡</span>
                    Hardware Routers Using {ipData.ip} Gateway
                  </h2>
                  <p class="text-xs text-[#5f6368] dark:text-[#9aa0a6]">
                    Verified factory default credentials and model specifications
                  </p>
                </div>
                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-[#e8f0fe] dark:bg-[#1a3a60] text-[#1a73e8] dark:text-[#8ab4f8] border border-[#d2e3fc] dark:border-[#1a73e8] w-fit">
                  {associatedModels.length} Models
                </span>
              </div>
              <PasswordTable models={associatedModels} />
            </section>
          )}

          <!-- Mid Ad Slot -->
          <AdBanner format="horizontal" />

          {/* Other Gateway IP Addresses Grid */}
          <section class="g-card p-4 sm:p-5 space-y-3">
            <h2 class="text-xs font-bold uppercase tracking-wider text-[#5f6368] dark:text-[#9aa0a6]">
              Other Popular Router Gateway IP Addresses
            </h2>
            <div class="flex flex-wrap gap-2">
              {otherIps.map(item => (
                <a 
                  href={`${base}/${item.slug}`}
                  class="g-chip font-mono text-[11px]"
                >
                  {item.ip}
                </a>
              ))}
            </div>
          </section>
        </article>
      );
    })()}

    {/* ========================================================= */}
    {/* VIEW 2: BRAND HUB PAGE */}
    {/* ========================================================= */}
    {type === 'brand' && (() => {
      const brandData = data as Brand;
      const brandModels = getModelsForBrand(brandData.slug);
      const finalModels = brandModels.length > 0 ? brandModels : brandData.models;
      const brandIpSlug = getIpSlug(brandData.defaultIp);
      const otherBrands = BRANDS.filter(b => b.slug !== brandData.slug);

      return (
        <article class="space-y-6">
          {/* Breadcrumbs */}
          <nav class="flex items-center gap-1.5 text-xs text-[#5f6368] dark:text-[#9aa0a6]">
            <a href={`${base}/`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">Home</a>
            <span>/</span>
            <a href={`${base}/brands`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">Brands</a>
            <span>/</span>
            <span class="text-[#202124] dark:text-[#e8eaed] font-medium">{brandData.name}</span>
          </nav>

          {/* Hero Header Card */}
          <header class="g-card p-5 sm:p-7 space-y-5">
            <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-[#e8eaed] dark:border-[#3c4043] pb-4">
              <div class="flex items-start gap-4">
                <div class="w-14 h-14 rounded-2xl bg-gradient-to-br from-[#1a73e8] to-[#1557b0] text-white flex items-center justify-center text-lg font-bold shadow-xs flex-shrink-0">
                  {brandData.name.slice(0, 2).toUpperCase()}
                </div>
                <div>
                  <div class="inline-flex items-center gap-1.5 px-3 py-0.5 rounded-full text-[11px] font-bold bg-[#e6f4ea] dark:bg-[#133e24] text-[#34a853] dark:text-[#81c995] border border-[#ceead6] dark:border-[#34a853] mb-1.5">
                    <span class="w-1.5 h-1.5 rounded-full bg-[#34a853]"></span>
                    Verified Manufacturer Hub
                  </div>
                  <h1 class="text-2xl sm:text-3xl font-bold text-[#202124] dark:text-white tracking-tight">
                    {brandData.name} Router Login & Default Passwords
                  </h1>
                  <p class="text-xs sm:text-sm text-[#5f6368] dark:text-[#9aa0a6] mt-1 max-w-2xl leading-relaxed">
                    {brandData.description}
                  </p>
                </div>
              </div>

              <div class="flex-shrink-0 flex items-center gap-2">
                <a 
                  href={`http://${brandData.defaultIp}`} 
                  target="_blank" 
                  rel="noopener noreferrer"
                  class="g-btn-primary"
                >
                  <span>Open {brandData.defaultIp}</span>
                  <span>↗</span>
                </a>
              </div>
            </div>

            {/* Default Login Credentials Action Grid */}
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Default Gateway IP</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">
                    {brandIpSlug ? (
                      <a href={`${base}/${brandIpSlug}`} class="text-[#1a73e8] dark:text-[#8ab4f8] hover:underline">
                        {brandData.defaultIp}
                      </a>
                    ) : (
                      brandData.defaultIp
                    )}
                  </div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={brandData.defaultIp}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Default Username</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">
                    {brandData.defaultUser || '(blank)'}
                  </div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={brandData.defaultUser}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Default Password</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">
                    {brandData.defaultPass || '(blank)'}
                  </div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={brandData.defaultPass}
                >
                  📋 Copy
                </button>
              </div>
            </div>
          </header>

          {/* Interactive Hardware Models Directory */}
          <section class="g-card p-5 sm:p-6 space-y-4">
            <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3 border-b border-[#e8eaed] dark:border-[#3c4043] pb-3">
              <div>
                <h2 class="text-base sm:text-lg font-bold text-[#202124] dark:text-white flex items-center gap-2">
                  <span>📟</span>
                  {brandData.name} Hardware Models Catalog
                </h2>
                <p class="text-xs text-[#5f6368] dark:text-[#9aa0a6]">
                  Select any model below to view technical specs, port configuration, and setup guide
                </p>
              </div>
              <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-[#e6f4ea] dark:bg-[#133e24] text-[#34a853] dark:text-[#81c995] border border-[#ceead6] dark:border-[#34a853] w-fit">
                {finalModels.length} Models Indexed
              </span>
            </div>

            <PasswordTable models={finalModels} brandName={brandData.name} brandSlug={brandData.slug} />
          </section>

          <!-- Mid Ad Slot -->
          <AdBanner format="horizontal" />

          {/* Step-by-Step Configuration Guide */}
          <section class="g-card p-5 sm:p-6 space-y-4">
            <div class="flex items-center gap-2 border-b border-[#e8eaed] dark:border-[#3c4043] pb-3">
              <span class="text-lg">📋</span>
              <h2 class="text-base sm:text-lg font-bold text-[#202124] dark:text-white">
                How to Log In & Configure Your {brandData.name} Router
              </h2>
            </div>
            <ol class="space-y-3">
              {brandData.guide.map((step, idx) => (
                <li class="flex items-start gap-3.5 text-xs sm:text-sm text-[#3c4043] dark:text-[#e8eaed] p-3.5 rounded-2xl bg-[#f8fafd] dark:bg-[#202124] border border-[#e8eaed] dark:border-[#3c4043]">
                  <span class="flex-shrink-0 w-7 h-7 rounded-full bg-[#1a73e8] text-white font-bold flex items-center justify-center text-xs shadow-xs">
                    {idx + 1}
                  </span>
                  <span class="pt-0.5 leading-relaxed">{step}</span>
                </li>
              ))}
            </ol>
          </section>

          {/* Factory Reset Instructions */}
          <section class="p-5 sm:p-6 rounded-2xl sm:rounded-3xl bg-[#fef7e0] dark:bg-[#332a00]/40 border border-[#fbbc04]/60 dark:border-[#fbbc04]/30 space-y-3">
            <div class="flex items-center gap-2 text-[#b06000] dark:text-[#fdd663] font-bold text-sm sm:text-base">
              <span>🔄</span>
              <span>How to Hard Reset {brandData.name} Router to Factory Defaults</span>
            </div>
            <p class="text-xs text-[#5f6368] dark:text-[#e8eaed] leading-relaxed">
              If you forgot your customized admin password or cannot reach the web portal, locate the physical <strong>RESET</strong> pinhole button on the back of your {brandData.name} router. While powered on, press and hold the button with a paperclip for <strong>10 to 15 seconds</strong> until the power LED begins flashing. Release the button and wait 2 minutes for the unit to reboot with factory default credentials (<strong>User: {brandData.defaultUser || 'admin'} / Pass: {brandData.defaultPass || 'admin'}</strong>).
            </p>
          </section>

          {/* Other Popular Brands Grid */}
          <section class="g-card p-4 sm:p-5 space-y-3">
            <h2 class="text-xs font-bold uppercase tracking-wider text-[#5f6368] dark:text-[#9aa0a6]">
              Other Popular Router Brands
            </h2>
            <div class="flex flex-wrap gap-2">
              {otherBrands.map(b => (
                <a 
                  href={`${base}/${b.slug}`}
                  class="g-chip"
                >
                  {b.name}
                </a>
              ))}
            </div>
          </section>
        </article>
      );
    })()}

    {/* ========================================================= */}
    {/* VIEW 3: MODEL DEDICATED SPECIFICATION PAGE */}
    {/* ========================================================= */}
    {type === 'model' && (() => {
      const model = data as RouterModel;
      const brandSlug = model.brandSlug || getBrandSlug(model.brand);
      const ipSlug = getIpSlug(model.ip);
      const siblingModels = ROUTER_MODELS.filter(m => m.brandSlug === model.brandSlug && m.slug !== model.slug).slice(0, 6);

      return (
        <article class="space-y-6">
          {/* Breadcrumb Navigation */}
          <nav class="flex items-center gap-1.5 text-xs text-[#5f6368] dark:text-[#9aa0a6] flex-wrap">
            <a href={`${base}/`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">Home</a>
            <span>/</span>
            <a href={`${base}/routers`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">Routers</a>
            <span>/</span>
            {brandSlug ? (
              <a href={`${base}/${brandSlug}`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">{model.brand}</a>
            ) : (
              <span>{model.brand}</span>
            )}
            <span>/</span>
            <span class="text-[#202124] dark:text-[#e8eaed] font-medium">{model.model}</span>
          </nav>

          {/* Model Header Card */}
          <header class="g-card p-5 sm:p-7 space-y-5">
            <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-[#e8eaed] dark:border-[#3c4043] pb-4">
              <div>
                <div class="flex items-center gap-2 flex-wrap mb-2.5">
                  <span class="px-3 py-0.5 rounded-full text-[11px] font-bold bg-[#e8f0fe] dark:bg-[#1a3a60] text-[#1a73e8] dark:text-[#8ab4f8] border border-[#d2e3fc] dark:border-[#1a73e8]">
                    {model.category}
                  </span>
                  {model.wifiStandard && (
                    <span class="px-3 py-0.5 rounded-full text-[11px] font-bold bg-[#fce8e6] dark:bg-[#4c1d1a] text-[#ea4335] dark:text-[#f28b82] border border-[#fad2cf] dark:border-[#ea4335]">
                      {model.wifiStandard}
                    </span>
                  )}
                  {model.speedRating && (
                    <span class="px-3 py-0.5 rounded-full text-[11px] font-bold bg-[#e6f4ea] dark:bg-[#133e24] text-[#34a853] dark:text-[#81c995] border border-[#ceead6] dark:border-[#34a853]">
                      {model.speedRating}
                    </span>
                  )}
                </div>

                <h1 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-[#202124] dark:text-white tracking-tight">
                  {model.brand} {model.model}
                </h1>
                <p class="text-xs sm:text-sm text-[#5f6368] dark:text-[#9aa0a6] mt-1 max-w-2xl leading-relaxed">
                  {model.useCase}
                </p>
              </div>

              <div class="flex-shrink-0">
                <a 
                  href={`http://${model.ip}`} 
                  target="_blank" 
                  rel="noopener noreferrer"
                  class="g-btn-primary"
                >
                  <span>Open http://{model.ip}</span>
                  <span class="text-xs">↗</span>
                </a>
              </div>
            </div>

            {/* Default Login Credentials Action Grid */}
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Gateway IP</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">
                    {ipSlug ? (
                      <a href={`${base}/${ipSlug}`} class="text-[#1a73e8] dark:text-[#8ab4f8] hover:underline">
                        {model.ip}
                      </a>
                    ) : (
                      model.ip
                    )}
                  </div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={model.ip}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Factory Username</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">
                    {model.username || '(blank)'}
                  </div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={model.username}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Factory Password</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">
                    {model.password || '(blank)'}
                  </div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={model.password}
                >
                  📋 Copy
                </button>
              </div>
            </div>
          </header>

          {/* Technical Specifications Matrix */}
          <section class="g-card p-5 sm:p-6 space-y-4">
            <div class="flex items-center justify-between border-b border-[#e8eaed] dark:border-[#3c4043] pb-3">
              <h2 class="text-base sm:text-lg font-bold text-[#202124] dark:text-white flex items-center gap-2">
                <span>⚙️</span>
                Technical Hardware Specifications
              </h2>
              <span class="text-xs font-semibold text-[#5f6368] dark:text-[#9aa0a6]">
                {model.series || `${model.brand} Hardware`}
              </span>
            </div>

            <div class="overflow-x-auto rounded-2xl border border-[#e8eaed] dark:border-[#3c4043]">
              <table class="w-full text-xs text-left">
                <tbody class="divide-y divide-[#e8eaed] dark:divide-[#3c4043]">
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] w-1/3 bg-[#f8fafd] dark:bg-[#202124]">Manufacturer</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">
                      {brandSlug ? (
                        <a href={`${base}/${brandSlug}`} class="text-[#1a73e8] dark:text-[#8ab4f8] hover:underline">
                          {model.brand}
                        </a>
                      ) : (
                        model.brand
                      )}
                    </td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Product Series</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">{model.series || `${model.brand} Series`}</td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Device Class / Category</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">
                      <span class="px-2.5 py-0.5 rounded-full bg-[#f1f3f4] dark:bg-[#303134] text-[#3c4043] dark:text-[#e8eaed] font-medium">
                        {model.category}
                      </span>
                    </td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Wireless Standard</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">{model.wifiStandard || 'N/A (Wired Gateway)'}</td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Speed Rating</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">{model.speedRating || 'Gigabit Class'}</td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Ethernet & Fiber Ports</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">{model.ports}</td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Routing / Firewall Throughput</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">{model.throughput || 'Full Line-Rate Gigabit'}</td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Operating System / Firmware</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">{model.os || `${model.brand} Firmware`}</td>
                  </tr>
                  <tr class="hover:bg-[#f8fafd] dark:hover:bg-[#303134]/40">
                    <th class="py-3 px-4 font-bold text-[#5f6368] dark:text-[#9aa0a6] bg-[#f8fafd] dark:bg-[#202124]">Default Gateway URL</th>
                    <td class="py-3 px-4 font-semibold text-[#202124] dark:text-white">
                      <a href={`http://${model.ip}`} target="_blank" rel="noopener noreferrer" class="text-[#1a73e8] dark:text-[#8ab4f8] hover:underline">
                        http://{model.ip} ↗
                      </a>
                    </td>
                  </tr>
                </tbody>
              </table>
            </div>
          </section>

          <!-- Mid Ad Slot -->
          <AdBanner format="horizontal" />

          {/* Setup Walkthrough */}
          <section class="g-card p-5 sm:p-6 space-y-4">
            <div class="flex items-center gap-2 border-b border-[#e8eaed] dark:border-[#3c4043] pb-3">
              <span class="text-lg">📖</span>
              <h2 class="text-base sm:text-lg font-bold text-[#202124] dark:text-white">
                How to Log In & Set Up Your {model.brand} {model.model}
              </h2>
            </div>
            <ol class="space-y-3">
              <li class="flex items-start gap-3.5 text-xs sm:text-sm text-[#3c4043] dark:text-[#e8eaed] p-3.5 rounded-2xl bg-[#f8fafd] dark:bg-[#202124] border border-[#e8eaed] dark:border-[#3c4043]">
                <span class="flex-shrink-0 w-7 h-7 rounded-full bg-[#1a73e8] text-white font-bold flex items-center justify-center text-xs shadow-xs">
                  1
                </span>
                <span class="pt-0.5 leading-relaxed">
                  <strong>Connect Your Device:</strong> Plug your PC into any LAN port on the <strong>{model.brand} {model.model}</strong> using an Ethernet cable, or connect to the default Wi-Fi SSID printed on the router label.
                </span>
              </li>
              <li class="flex items-start gap-3.5 text-xs sm:text-sm text-[#3c4043] dark:text-[#e8eaed] p-3.5 rounded-2xl bg-[#f8fafd] dark:bg-[#202124] border border-[#e8eaed] dark:border-[#3c4043]">
                <span class="flex-shrink-0 w-7 h-7 rounded-full bg-[#34a853] text-white font-bold flex items-center justify-center text-xs shadow-xs">
                  2
                </span>
                <span class="pt-0.5 leading-relaxed">
                  <strong>Navigate to Gateway IP:</strong> Launch your web browser and enter <code class="px-1.5 py-0.5 rounded bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] font-mono text-xs font-bold text-[#1a73e8] dark:text-[#8ab4f8]">http://{model.ip}</code> into the address bar.
                </span>
              </li>
              <li class="flex items-start gap-3.5 text-xs sm:text-sm text-[#3c4043] dark:text-[#e8eaed] p-3.5 rounded-2xl bg-[#f8fafd] dark:bg-[#202124] border border-[#e8eaed] dark:border-[#3c4043]">
                <span class="flex-shrink-0 w-7 h-7 rounded-full bg-[#f9ab00] text-white font-bold flex items-center justify-center text-xs shadow-xs">
                  3
                </span>
                <span class="pt-0.5 leading-relaxed">
                  <strong>Enter Factory Credentials:</strong> Enter username <strong class="font-mono text-[#202124] dark:text-white">"{model.username}"</strong> and password <strong class="font-mono text-[#202124] dark:text-white">"{model.password}"</strong>, then click Login.
                </span>
              </li>
              <li class="flex items-start gap-3.5 text-xs sm:text-sm text-[#3c4043] dark:text-[#e8eaed] p-3.5 rounded-2xl bg-[#f8fafd] dark:bg-[#202124] border border-[#e8eaed] dark:border-[#3c4043]">
                <span class="flex-shrink-0 w-7 h-7 rounded-full bg-[#ea4335] text-white font-bold flex items-center justify-center text-xs shadow-xs">
                  4
                </span>
                <span class="pt-0.5 leading-relaxed">
                  <strong>Configure Security & Password:</strong> Follow the on-screen Quick Setup wizard to customize your Wi-Fi SSID, set WPA3/WPA2-AES encryption, and establish a new master administrator password.
                </span>
              </li>
            </ol>
          </section>

          {/* Sibling Hardware Models Interlink */}
          {siblingModels.length > 0 && (
            <section class="g-card p-4 sm:p-5 space-y-3">
              <h2 class="text-xs font-bold uppercase tracking-wider text-[#5f6368] dark:text-[#9aa0a6]">
                Related {model.brand} Hardware Models
              </h2>
              <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-2.5">
                {siblingModels.map(s => (
                  <a 
                    href={`${base}/${s.slug}`}
                    class="p-3.5 rounded-2xl bg-[#f8fafd] dark:bg-[#202124] hover:bg-[#e8f0fe] dark:hover:bg-[#1a3a60] border border-[#e8eaed] dark:border-[#3c4043] transition-colors block space-y-1.5"
                  >
                    <div class="text-xs font-bold text-[#202124] dark:text-white truncate">
                      {s.model}
                    </div>
                    <div class="text-[11px] text-[#5f6368] dark:text-[#9aa0a6] flex items-center justify-between">
                      <span class="truncate max-w-[120px]">{s.category}</span>
                      <span class="font-mono text-[#1a73e8] dark:text-[#8ab4f8] font-bold">{s.ip}</span>
                    </div>
                  </a>
                ))}
              </div>
            </section>
          )}
        </article>
      );
    })()}

    {/* ========================================================= */}
    {/* VIEW 4: ISP FIBER GATEWAY PAGE */}
    {/* ========================================================= */}
    {type === 'isp' && (() => {
      const ispData = data as Isp;
      const otherIsps = ISPS.filter(i => i.slug !== ispData.slug);

      return (
        <article class="space-y-6">
          {/* Breadcrumb Navigation */}
          <nav class="flex items-center gap-1.5 text-xs text-[#5f6368] dark:text-[#9aa0a6]">
            <a href={`${base}/`} class="hover:text-[#1a73e8] dark:hover:text-[#8ab4f8] transition-colors">Home</a>
            <span>/</span>
            <span class="text-[#202124] dark:text-[#e8eaed] font-medium">{ispData.name}</span>
          </nav>

          {/* Hero Header Card */}
          <header class="g-card p-5 sm:p-7 space-y-5">
            <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-[#e8eaed] dark:border-[#3c4043] pb-4">
              <div>
                <div class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold bg-[#e6f4ea] dark:bg-[#133e24] text-[#34a853] dark:text-[#81c995] border border-[#ceead6] dark:border-[#34a853] mb-2">
                  <span class="w-1.5 h-1.5 rounded-full bg-[#34a853]"></span>
                  Fiber Broadband Provider
                </div>
                <h1 class="text-2xl sm:text-3xl font-bold text-[#202124] dark:text-white tracking-tight">
                  {ispData.name} Router Login & Setup Guide
                </h1>
                <p class="text-xs sm:text-sm text-[#5f6368] dark:text-[#9aa0a6] mt-1 max-w-2xl leading-relaxed">
                  {ispData.description}
                </p>
              </div>

              <div class="flex-shrink-0">
                <a 
                  href={`http://${ispData.defaultIp}`} 
                  target="_blank" 
                  rel="noopener noreferrer"
                  class="g-btn-primary"
                >
                  <span>Open http://{ispData.defaultIp}</span>
                  <span class="text-xs">↗</span>
                </a>
              </div>
            </div>

            {/* Default Login Credentials Action Grid */}
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Gateway IP</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">{ispData.defaultIp}</div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={ispData.defaultIp}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Default User</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">{ispData.defaultUser}</div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={ispData.defaultUser}
                >
                  📋 Copy
                </button>
              </div>

              <div class="bg-[#f8fafd] dark:bg-[#202124] rounded-2xl p-3.5 border border-[#e8eaed] dark:border-[#3c4043] flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-bold text-[#5f6368] dark:text-[#9aa0a6] tracking-wider">Default Pass</div>
                  <div class="text-sm font-mono font-bold text-[#202124] dark:text-white mt-0.5">{ispData.defaultPass}</div>
                </div>
                <button 
                  type="button" 
                  class="copy-btn text-[11px] font-bold px-3 py-1 rounded-full bg-white dark:bg-[#303134] border border-[#dadce0] dark:border-[#5f6368] text-[#3c4043] dark:text-[#e8eaed] hover:bg-[#f1f3f4] dark:hover:bg-[#3c4043] transition-all cursor-pointer shadow-2xs"
                  data-copy={ispData.defaultPass}
                >
                  📋 Copy
                </button>
              </div>
            </div>
          </header>

          {/* Step-by-Step ONT Setup Guide */}
          <section class="g-card p-5 sm:p-6 space-y-4">
            <div class="flex items-center gap-2 border-b border-[#e8eaed] dark:border-[#3c4043] pb-3">
              <span class="text-lg">🛠️</span>
              <h2 class="text-base sm:text-lg font-bold text-[#202124] dark:text-white">
                Step-by-Step {ispData.name} ONT Setup Guide
              </h2>
            </div>
            <ol class="space-y-3">
              {ispData.instructions.map((step, idx) => (
                <li class="flex items-start gap-3.5 text-xs sm:text-sm text-[#3c4043] dark:text-[#e8eaed] p-3.5 rounded-2xl bg-[#f8fafd] dark:bg-[#202124] border border-[#e8eaed] dark:border-[#3c4043]">
                  <span class="flex-shrink-0 w-7 h-7 rounded-full bg-[#34a853] text-white font-bold flex items-center justify-center text-xs shadow-xs">
                    {idx + 1}
                  </span>
                  <span class="pt-0.5 leading-relaxed">{step}</span>
                </li>
              ))}
            </ol>
          </section>

          {/* Other Fiber Broadband Providers Grid */}
          <section class="g-card p-4 sm:p-5 space-y-3">
            <h2 class="text-xs font-bold uppercase tracking-wider text-[#5f6368] dark:text-[#9aa0a6]">
              Other Fiber Broadband Providers
            </h2>
            <div class="flex flex-wrap gap-2">
              {otherIsps.map(i => (
                <a 
                  href={`${base}/${i.slug}`}
                  class="g-chip"
                >
                  {i.name} ({i.defaultIp})
                </a>
              ))}
            </div>
          </section>
        </article>
      );
    })()}

    {/* 2-Column Cross Linking Grid (Placed Before FAQ) */}
    <RelatedLinksGrid currentSlug={data.slug} />

    {/* ========================================================= */}
    {/* FAQ ACCORDION SECTION (STRICTLY JUST ABOVE FOOTER) */}
    {/* ========================================================= */}
    <section class="g-card p-5 sm:p-7 space-y-4">
      <div class="flex items-center gap-2 border-b border-[#e8eaed] dark:border-[#3c4043] pb-3">
        <span class="text-lg">❓</span>
        <div>
          <h2 class="text-base sm:text-lg font-bold text-[#202124] dark:text-white">
            Frequently Asked Questions & Answers
          </h2>
          <p class="text-xs text-[#5f6368] dark:text-[#9aa0a6]">
            Common troubleshooting solutions and login assistance
          </p>
        </div>
      </div>

      <div class="space-y-3">
        {type === 'ip' && (() => {
          const ip = data as GatewayIp;
          const faqsList = ip.faqs && ip.faqs.length > 0 ? ip.faqs : [
            { question: `What is the default login for ${ip.ip}?`, answer: `The default username is typically "${ip.defaultUser}" and password is "${ip.defaultPass}".` },
            { question: `Why is ${ip.ip} not loading in my browser?`, answer: `Make sure your device is directly connected to the router via Wi-Fi or Ethernet, and verify that mobile data or VPNs are turned off.` }
          ];

          return faqsList.map(faq => (
            <details class="group border border-[#dadce0] dark:border-[#3c4043] rounded-2xl p-4 open:bg-[#f8fafd] dark:open:bg-[#303134]/50 hover:border-[#bdc1c6] dark:hover:border-[#5f6368] transition-colors">
              <summary class="font-bold text-[#202124] dark:text-white cursor-pointer list-none flex justify-between items-center text-xs sm:text-sm">
                <span class="flex items-center gap-2">
                  <span class="text-[#1a73e8] dark:text-[#8ab4f8] font-bold">Q.</span>
                  {faq.question}
                </span>
                <span class="text-[#9aa0a6] group-open:rotate-180 group-open:text-[#1a73e8] dark:group-open:text-[#8ab4f8] transition-transform text-xs flex-shrink-0 ml-2">▼</span>
              </summary>
              <p class="text-xs text-[#5f6368] dark:text-[#bdc1c6] mt-2.5 leading-relaxed border-t border-[#e8eaed] dark:border-[#3c4043] pt-2.5">
                {faq.answer}
              </p>
            </details>
          ));
        })()}

        {type === 'brand' && (() => {
          const brand = data as Brand;
          const brandFaqs = [
            {
              question: `What is the default IP address for ${brand.name} routers?`,
              answer: `Most ${brand.name} routers use ${brand.defaultIp} as their factory default gateway management address.`
            },
            {
              question: `What is the factory default login password for ${brand.name}?`,
              answer: `The standard factory username is "${brand.defaultUser}" and default password is "${brand.defaultPass}". Check your physical router label if these don't work.`
            },
            {
              question: `How do I factory reset my ${brand.name} router if I forgot the password?`,
              answer: `Press and hold the physical RESET pinhole button on the back of your ${brand.name} device with a paperclip for 10-15 seconds until all LED lights blink, restoring default credentials.`
            }
          ];

          return brandFaqs.map(faq => (
            <details class="group border border-[#dadce0] dark:border-[#3c4043] rounded-2xl p-4 open:bg-[#f8fafd] dark:open:bg-[#303134]/50 hover:border-[#bdc1c6] dark:hover:border-[#5f6368] transition-colors">
              <summary class="font-bold text-[#202124] dark:text-white cursor-pointer list-none flex justify-between items-center text-xs sm:text-sm">
                <span class="flex items-center gap-2">
                  <span class="text-[#1a73e8] dark:text-[#8ab4f8] font-bold">Q.</span>
                  {faq.question}
                </span>
                <span class="text-[#9aa0a6] group-open:rotate-180 group-open:text-[#1a73e8] dark:group-open:text-[#8ab4f8] transition-transform text-xs flex-shrink-0 ml-2">▼</span>
              </summary>
              <p class="text-xs text-[#5f6368] dark:text-[#bdc1c6] mt-2.5 leading-relaxed border-t border-[#e8eaed] dark:border-[#3c4043] pt-2.5">
                {faq.answer}
              </p>
            </details>
          ));
        })()}

        {type === 'model' && (() => {
          const model = data as RouterModel;
          const modelFaqs = [
            {
              question: `What is the default login for ${model.brand} ${model.model}?`,
              answer: `The default IP address is http://${model.ip}, default username is "${model.username}", and default password is "${model.password}".`
            },
            {
              question: `What ports and speeds does ${model.brand} ${model.model} support?`,
              answer: `The ${model.brand} ${model.model} is equipped with ${model.ports} with an overall speed rating of ${model.speedRating || 'Gigabit Class'}.`
            },
            {
              question: `How do I reset my ${model.brand} ${model.model} to factory settings?`,
              answer: `Press and hold the physical Reset button on the ${model.brand} ${model.model} for 10 to 15 seconds until the power LED blinks, resetting all settings to factory default.`
            }
          ];

          return modelFaqs.map(faq => (
            <details class="group border border-[#dadce0] dark:border-[#3c4043] rounded-2xl p-4 open:bg-[#f8fafd] dark:open:bg-[#303134]/50 hover:border-[#bdc1c6] dark:hover:border-[#5f6368] transition-colors">
              <summary class="font-bold text-[#202124] dark:text-white cursor-pointer list-none flex justify-between items-center text-xs sm:text-sm">
                <span class="flex items-center gap-2">
                  <span class="text-[#1a73e8] dark:text-[#8ab4f8] font-bold">Q.</span>
                  {faq.question}
                </span>
                <span class="text-[#9aa0a6] group-open:rotate-180 group-open:text-[#1a73e8] dark:group-open:text-[#8ab4f8] transition-transform text-xs flex-shrink-0 ml-2">▼</span>
              </summary>
              <p class="text-xs text-[#5f6368] dark:text-[#bdc1c6] mt-2.5 leading-relaxed border-t border-[#e8eaed] dark:border-[#3c4043] pt-2.5">
                {faq.answer}
              </p>
            </details>
          ));
        })()}

        {type === 'isp' && (() => {
          const isp = data as Isp;
          const ispFaqs = [
            {
              question: `How do I log in to my ${isp.name} fiber router admin portal?`,
              answer: `Connect to your ${isp.name} Wi-Fi network, navigate to http://${isp.defaultIp} in your browser, and enter username "${isp.defaultUser}" and password "${isp.defaultPass}".`
            },
            {
              question: `What should I do if the LOS light on my ${isp.name} router is blinking red?`,
              answer: `A blinking red LOS (Loss of Signal) light indicates an optical fiber cable disconnection or an ISP outage. Verify the thin yellow fiber cable is firmly seated, and contact ${isp.name} support if the issue persists.`
            },
            {
              question: `How can I change my ${isp.name} Wi-Fi password and network name?`,
              answer: `Log in to http://${isp.defaultIp}, go to Network/WLAN -> 2.4G/5G Settings, update the SSID and WPA Pre-Shared Key, then click Apply.`
            }
          ];

          return ispFaqs.map(faq => (
            <details class="group border border-[#dadce0] dark:border-[#3c4043] rounded-2xl p-4 open:bg-[#f8fafd] dark:open:bg-[#303134]/50 hover:border-[#bdc1c6] dark:hover:border-[#5f6368] transition-colors">
              <summary class="font-bold text-[#202124] dark:text-white cursor-pointer list-none flex justify-between items-center text-xs sm:text-sm">
                <span class="flex items-center gap-2">
                  <span class="text-[#1a73e8] dark:text-[#8ab4f8] font-bold">Q.</span>
                  {faq.question}
                </span>
                <span class="text-[#9aa0a6] group-open:rotate-180 group-open:text-[#1a73e8] dark:group-open:text-[#8ab4f8] transition-transform text-xs flex-shrink-0 ml-2">▼</span>
              </summary>
              <p class="text-xs text-[#5f6368] dark:text-[#bdc1c6] mt-2.5 leading-relaxed border-t border-[#e8eaed] dark:border-[#3c4043] pt-2.5">
                {faq.answer}
              </p>
            </details>
          ));
        })()}
      </div>
    </section>

  </div>
</BaseLayout>

<script>
  // Single-click copy handler for buttons with data-copy attribute
  document.querySelectorAll<HTMLButtonElement>('.copy-btn').forEach(button => {
    button.addEventListener('click', () => {
      const textToCopy = button.getAttribute('data-copy');
      if (!textToCopy) return;

      navigator.clipboard.writeText(textToCopy).then(() => {
        const originalText = button.innerHTML;
        button.innerHTML = '✓ Copied';
        button.classList.add('bg-[#34a853]', 'text-white', 'border-[#34a853]');
        button.classList.remove('bg-white', 'dark:bg-[#303134]', 'text-[#3c4043]', 'dark:text-[#e8eaed]');

        setTimeout(() => {
          button.innerHTML = originalText;
          button.classList.remove('bg-[#34a853]', 'text-white', 'border-[#34a853]');
          button.classList.add('bg-white', 'dark:bg-[#303134]', 'text-[#3c4043]', 'dark:text-[#e8eaed]');
        }, 1500);
      });
    });
  });
</script>
