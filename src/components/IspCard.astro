---
import type { Isp } from '../lib/types';

interface Props {
  data: Isp;
}

const { data } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<a 
  href={`${base}/${data.slug}`} 
  class="group bg-white rounded-xl border border-slate-200/80 p-3.5 sm:p-4 shadow-card hover:shadow-card-hover hover:border-slate-300 transition-all flex flex-col justify-between"
>
  <div>
    <div class="flex items-start justify-between gap-2 mb-2">
      <div class="flex items-center gap-2.5">
        <div class="w-9 h-9 rounded-lg bg-emerald-700 text-white flex items-center justify-center font-bold text-xs font-mono group-hover:bg-emerald-800 transition-colors shadow-2xs">
          {data.name.slice(0, 2).toUpperCase()}
        </div>
        <div>
          <h3 class="font-bold text-slate-900 text-sm group-hover:text-emerald-700 transition-colors">
            {data.name}
          </h3>
          <span class="text-[11px] text-slate-500 font-medium">
            Fiber Broadband
          </span>
        </div>
      </div>
      <span class="text-[11px] font-mono font-semibold px-2 py-0.5 rounded bg-emerald-50 text-emerald-800">
        {data.defaultIp}
      </span>
    </div>

    <p class="text-xs text-slate-500 line-clamp-2 leading-relaxed mb-3">
      {data.description}
    </p>
  </div>

  <div class="pt-2 border-t border-slate-100 flex items-center justify-between text-[11px] font-medium text-slate-500">
    <span>Login: <strong class="text-slate-800 font-mono">{data.defaultUser}</strong></span>
    <span class="text-slate-900 group-hover:text-emerald-700 font-semibold flex items-center gap-0.5">
      Setup Guide →
    </span>
  </div>
</a>
